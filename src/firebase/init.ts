// This file is deprecated and replaced by the standard initialization in src/firebase/index.ts.
// It is kept empty to avoid build breaks if some imports still point here, 
// but you should use import { useUser, useFirestore, etc } from '@/firebase';
export {};
